// FLAGS — simulation/live switches. Flipping one boolean here is the only
// code change needed to graduate an integration from simulation to live.

export const FLAGS = {
  annotationsSimulated: true,
  searchExternal: false,
}
